extends ../layout

block content
    include ../mixins/articleCard
    div.articles-header
        div.article-header-title Here's What's New
        div
            if user && user.is('author')
                a.button.primary(href='/article/add') New Post

    ul.article-list
        each article in articles
            li
                +articleCard(article)

                //todo make this work or find another way to display articles on profile